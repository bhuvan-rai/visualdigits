# visualdigits

CURRENTLY WONT WORK ( and even if it could it would be BAD asf ) : 
NEED TO REPLACE H5 WITH KERAS DATASET 


info:
mnist.h5 -> data file ( keeps memory of model after training )
trainmain.py -> for training data ( currently accesses mnist dataset )
guimain.py -> for launching the program

basic pips needed to run:
1) pillow
2) numpy
3) keras
4) tensorflow
5) win32gui ( or pywin32 aslong as it works )
